<h3 class = "text-center medium-4 medium-centered columns"> <%= @summoner.name %> </h3>
<div class="circle">
  <ul>
  <% @teams.each do | t | %>

        <li><%= t.name %></li>
  <% end %>
  </ul>
</div>
