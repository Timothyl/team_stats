<h3 class = "text-center medium-4 medium-centered columns"> <%= @summoner.name %> </h3>
<div class="circle">
  <ul>
    <% @teams.each do | t | %>
        <li><%= link_to t.name, team_path(t) %></li>
    <% end %>
  </ul>
</div>
